---
phase: 03-instanced-rendering
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/components/game/hookshot/hookProjectile.tsx
  - apps/client/src/components/game/effects/instanced/InstancedHookHeads.tsx
autonomous: false
user_setup: []

must_haves:
  truths:
    - "10 hooks on screen render in single draw call for hook heads (not 10+)"
    - "Hook visual quality identical to pre-optimization (anchor shape, glowing crown, energy rope)"
    - "Rope lines still connect player position to hook head position"
    - "Hook extend/retract behavior works correctly (collision, damage, retraction)"
  artifacts:
    - path: "apps/client/src/components/game/effects/instanced/InstancedHookHeads.tsx"
      provides: "InstancedMesh-based hook head renderer"
      min_lines: 100
    - path: "apps/client/src/components/game/hookshot/hookProjectile.tsx"
      provides: "HookProjectile container using instanced renderer"
      exports: ["HookProjectile"]
  key_links:
    - from: "apps/client/src/components/game/hookshot/hookProjectile.tsx"
      to: "apps/client/src/store/gameStore.ts"
      via: "useGameStore(state => state.hookProjectiles)"
      pattern: "useGameStore.*hookProjectiles"
    - from: "apps/client/src/components/game/effects/instanced/InstancedHookHeads.tsx"
      to: "@react-three/drei"
      via: "import { Instances, Instance } from '@react-three/drei'"
      pattern: "from '@react-three/drei'"
---

<objective>
Implement InstancedMesh for hookshot hook heads using @react-three/drei's Instances API, reducing 10+ draw calls to a single draw call for all active hook heads while maintaining dynamic rope connections.

Purpose: Hook heads currently render as individual mesh components with 10 meshes per hook. Instancing consolidates all hook heads into a single GPU draw call while maintaining the anchor shape, glowing crown, and energy rope visualization.
Output: New InstancedHookHeads component using Drei Instances API, integrated into HookProjectile component
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-instanced-rendering/03-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

# Current hookshot rendering implementation
@apps/client/src/components/game/hookshot/hookProjectile.tsx

# Projectile type definitions
@apps/client/src/store/types.ts

# Shared geometries and materials
@apps/client/src/components/game/effectResources.ts
</context>

<tasks>

<task type="auto">
  <name>Create InstancedHookHeads component with Drei Instances API</name>
  <files>apps/client/src/components/game/effects/instanced/InstancedHookHeads.tsx</files>
  <action>
Create new instanced renderer for hook heads using @react-three/drei's Instances API:

1. Import dependencies:
   - `Instance, Instances` from '@react-three/drei'
   - `useFrame` from '@react-three/fiber'
   - `useGameStore` for hook data access
   - `getHookshotMaterials` from effectResources

2. Define interface for hook data:
   ```typescript
   interface HookHeadData {
     id: string;
     position: { x: number; y: number; z: number };
     velocity: { x: number; y: number; z: number };
     state: 'extending' | 'retracting';
     alive: boolean;
   }
   ```

3. Create InstancedHookHeads component:
   - Set `limit={MAX_HOOKS}` (20 for multi-player scenarios)
   - Use shared geometries from SHARED_GEOMETRIES (ring16, cylinder8, cone8, sphere8)
   - Use shared materials from getHookshotMaterials()
   - Render each hook head as 7 instances (ring, shaft, crown, leftArmShaft, leftArmFluke, rightArmShaft, rightArmFluke, tip, glow)

4. Position update logic in useFrame:
   - Access hooks non-reactively: `useGameStore.getState().hookProjectiles`
   - For each hook, update instance position based on hook.position
   - Calculate rotation to face velocity direction using quaternion
   - Set instance `scale={alive ? 1 : 0}` to hide removed hooks

5. DO NOT include point lights in instancing:
   - Point lights remain as individual elements (acceptable, only 1 per hook)
   - Point lights are not meshes and don't contribute to draw call count

6. Return Instances component with:
   - Shared geometries as children (ring16, cylinder8, cone8, sphere8)
   - Instance elements for each hook part (7 total)
   - Key prop for stable reconciliation based on hook.id

File location: Same directory as InstancedRockets (effects/instanced/)
  </action>
  <verify>
1. File exists at `apps/client/src/components/game/effects/instanced/InstancedHookHeads.tsx`
2. Component exports `InstancedHookHeads` function
3. Uses `Instances` and `Instance` from '@react-three/drei'
4. Accesses hook data via `useGameStore.getState()` (non-reactive)
5. Has `MAX_HOOKS = 20` constant
6. Updates instance positions and rotations in useFrame
7. Uses 7 instances per hook (matching original mesh count)
  </verify>
  <done>
InstancedHookHeads component created with Drei Instances API, rendering all hook heads in single draw call with position/rotation updates
  </done>
</task>

<task type="auto">
  <name>Refactor HookProjectile to use InstancedHookHeads with dynamic ropes</name>
  <files>apps/client/src/components/game/hookshot/hookProjectile.tsx</files>
  <action>
Refactor HookProjectile component to use InstancedHookHeads while maintaining dynamic rope rendering:

1. Import InstancedHookHeads:
   ```typescript
   import { InstancedHookHeads } from '../effects/instanced/InstancedHookHeads';
   ```

2. Simplify HookProjectile to focus on rope rendering:
   - Remove hook head mesh rendering (now handled by InstancedHookHeads)
   - Keep rope mesh rendering (ropeMainRef, ropeGlowRef, ropeCoreRef)
   - Keep point light (energy glow effect)
   - Keep useFrame logic for position updates and collision detection

3. Update HookProjectile return:
   ```tsx
   return (
     <group>
       {/* Hook heads rendered via instancing */}
       <InstancedHookHeads />
       {/* Rope layers - updated via refs, using shared materials */}
       <mesh ref={ropeMainRef} geometry={SHARED_GEOMETRIES.cylinder8} material={HOOK_MATERIALS.ropeMain} />
       <mesh ref={ropeGlowRef} geometry={SHARED_GEOMETRIES.cylinder8} material={HOOK_MATERIALS.ropeGlow} />
       <mesh ref={ropeCoreRef} geometry={SHARED_GEOMETRIES.cylinder8} material={HOOK_MATERIALS.ropeCore} />
     </group>
   );
   ```

4. Keep existing useFrame logic:
   - Hook position updates (extending/retracting)
   - Rope mesh scale and position updates (dynamic line to player)
   - Collision detection (terrain, NPCs)
   - Hook state management (hasHit, hookState)

5. Remove hook head meshes from JSX:
   - Delete the 7 mesh elements that form the hook head shape
   - Keep only the 3 rope meshes and point light

6. Update component props:
   - HookProjectile now receives hook data but only renders rope
   - Hook heads are rendered by InstancedHookHeads component

7. Add comment explaining architecture:
   ```typescript
   // Hybrid rendering: Hook heads use InstancedMesh (single draw call)
   // Rope layers render individually (dynamic sizing requires unique transforms)
   // Each hook = 1 instanced head (7 meshes) + 3 rope meshes + 1 point light
   ```

8. Note: Rope CANNOT be instanced because each rope has unique length/scale
   - Rope must stretch from player to hook head (variable length)
   - InstancedMesh requires identical geometry transforms
   - 3 rope meshes per hook is acceptable overhead (much less than 10+ meshes per hook)
  </action>
  <verify>
1. HookProjectile imports InstancedHookHeads
2. Renders `<InstancedHookHeads />` component
3. Hook head meshes removed from JSX (7 mesh elements deleted)
4. Rope meshes still render (ropeMainRef, ropeGlowRef, ropeCoreRef)
5. Point light still renders
6. useFrame logic preserved (position updates, collision, state)
7. Comment explaining hybrid rendering added
  </verify>
  <done>
HookProjectile refactored to use InstancedHookHeads for hook heads while maintaining dynamic rope rendering with individual rope meshes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete hybrid hookshot rendering system with InstancedHookHeads for heads and dynamic rope rendering for lines</what-built>
  <how-to-verify>
1. Start the game and fire hookshots (Hookshot basic attack)
2. Open r3f-perf monitor (top-left corner)
3. Fire 5+ hooks simultaneously
4. Observe the "calls" metric in r3f-perf:
   - Before optimization: 50+ calls (10 meshes × 5 hooks)
   - After optimization: ~22 calls (7 instanced + 3 rope meshes × 5 hooks)
   - Note: Ropes cannot be instanced due to variable length requirements
5. Verify hooks render correctly:
   - Hook head shape visible (anchor/crown design)
   - Glowing energy effect visible on hook
   - Rope line connects player hand to hook head
   - Rope stretches and retracts dynamically
6. Check visual quality matches pre-optimization:
   - Hook head colors unchanged (metallic shaft, glowing crown)
   - Rope colors unchanged (main, glow, core layers)
   - Hook size and proportions identical
   - Point light glow effect works
7. Test hook behavior:
   - Hooks extend forward and retract when hitting max distance
   - Hooks retract immediately on terrain collision
   - Hooks damage NPCs on contact
   - Hooks disappear when fully retracted

Type "approved" if hybrid instancing is working correctly, or describe issues to fix.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. r3f-perf shows ~60% reduction in draw calls during heavy hookshot use (7 meshes instanced, 3 rope per hook remain)
2. All hook head parts render correctly (anchor shape, crown, arms, tip, glow)
3. Rope lines connect player position to hook head position
4. Rope stretches and retracts dynamically
5. Hook extend/retract behavior unchanged (collision, damage, lifetime)
6. No visual artifacts (flickering, missing parts, incorrect colors)
7. Expired hooks hidden properly (scale=0)
</verification>

<success_criteria>
1. 10 hooks render hook heads in single draw call per part type (7 calls total)
2. r3f-perf shows 60%+ reduction in draw calls during hookshot spam
3. Visual quality identical to pre-optimization (hook shape, rope, glow)
4. Hook extend/retract behavior and collision detection unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-instanced-rendering/03-03-SUMMARY.md`
</output>
