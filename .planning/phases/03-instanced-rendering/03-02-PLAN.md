---
phase: 03-instanced-rendering
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/components/game/phantom/direBall.tsx
  - apps/client/src/components/game/effects/instanced/InstancedDireBalls.tsx
autonomous: false
user_setup: []

must_haves:
  truths:
    - "30 dire balls on screen render in single draw call (not 30+)"
    - "Dire ball visual quality identical to pre-optimization (swirling void patterns, cyan core)"
    - "Collision detection still works correctly (terrain, NPCs)"
    - "Shader animations (pulse, swirl, lightning) animate correctly"
  artifacts:
    - path: "apps/client/src/components/game/effects/instanced/InstancedDireBalls.tsx"
      provides: "InstancedMesh-based dire ball renderer"
      min_lines: 120
    - path: "apps/client/src/components/game/phantom/direBall.tsx"
      provides: "DireBalls container using instanced renderer"
      exports: ["DireBalls"]
  key_links:
    - from: "apps/client/src/components/game/phantom/direBall.tsx"
      to: "apps/client/src/store/slices/projectiles.ts"
      via: "useGameStore(state => state.direBalls)"
      pattern: "useGameStore.*direBalls"
    - from: "apps/client/src/components/game/effects/instanced/InstancedDireBalls.tsx"
      to: "@react-three/drei"
      via: "import { Instances, Instance } from '@react-three/drei'"
      pattern: "from '@react-three/drei'"
---

<objective>
Implement InstancedMesh for Phantom dire balls using @react-three/drei's Instances API, reducing 30+ draw calls to a single draw call for all active dire balls while preserving shader-based visual effects.

Purpose: Dire balls currently render as individual mesh components with complex shader materials. Instancing consolidates all dire balls into a single GPU draw call while maintaining the swirling void patterns, cyan core, and shader animations.
Output: New InstancedDireBalls component using Drei Instances API, integrated into DireBalls container
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-instanced-rendering/03-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

# Current dire ball rendering implementation
@apps/client/src/components/game/phantom/direBall.tsx

# Projectile type definitions
@apps/client/src/store/types.ts

# Shared geometries and materials
@apps/client/src/components/game/effectResources.ts
</context>

<tasks>

<task type="auto">
  <name>Create InstancedDireBalls component with Drei Instances API</name>
  <files>apps/client/src/components/game/effects/instanced/InstancedDireBalls.tsx</files>
  <action>
Create new instanced renderer for dire balls using @react-three/drei's Instances API:

1. Import dependencies:
   - `Instance, Instances` from '@react-three/drei'
   - `useFrame` from '@react-three/fiber'
   - `useGameStore` for dire ball data access
   - Shared materials from direBall.tsx (getSharedCoreMaterial, etc.)

2. Define interface for instance data:
   ```typescript
   interface DireBallInstance {
     id: string;
     position: { x: number; y: number; z: number };
     velocity: { x: number; y: number; z: number };
     startTime: number;
     ownerId: string;
     alive: boolean;
   }
   ```

3. Create InstancedDireBalls component:
   - Set `limit={MAX_DIRE_BALLS}` (30 for Phantom heavy combat scenarios)
   - Use sphere geometries: `sphereGeometry args={[BALL_RADIUS, 16, 16]}`
   - Use existing shared shader materials (coreMaterial, glowMaterial, particleMaterial)
   - Render each dire ball as 4 instances (core, outerGlow, innerCore, secondaryGlow)

4. Position update logic in useFrame:
   - Access dire balls non-reactively: `useGameStore.getState().direBalls`
   - Calculate position: `position = initialPosition + velocity * elapsed`
   - Check lifetime (3 seconds) and set `alive: false` when expired
   - Update shader uniforms: `coreMaterial.uniforms.time.value = elapsed`
   - Set instance `scale={alive ? 1 : 0}` to hide dead/expired balls

5. Handle rotation in useFrame:
   - Rotate core instance for dynamic effect: `coreRef.current.rotation.x += delta * 3`

6. DO NOT include particle trail in instancing:
   - Particles remain as individual `<points>` elements (only 1 per ball)
   - Particle overhead is minimal compared to mesh draw calls

7. Return Instances component with:
   - Shared shader materials (coreMaterial, glowMaterial)
   - Instance elements for each ball part (core, outerGlow, innerBrightCore, secondaryGlow)
   - Key prop for stable reconciliation based on ball.id

File location: Same directory as InstancedRockets (effects/instanced/)
  </action>
  <verify>
1. File exists at `apps/client/src/components/game/effects/instanced/InstancedDireBalls.tsx`
2. Component exports `InstancedDireBalls` function
3. Uses `Instances` and `Instance` from '@react-three/drei'
4. Accesses dire ball data via `useGameStore.getState()` (non-reactive)
5. Has `MAX_DIRE_BALLS = 30` constant
6. Updates shader uniforms (time) in useFrame hook
7. Updates instance positions and scale in useFrame
  </verify>
  <done>
InstancedDireBalls component created with Drei Instances API, rendering all dire balls in single draw call with shader animation updates
  </done>
</task>

<task type="auto">
  <name>Integrate InstancedDireBalls into DireBalls container</name>
  <files>apps/client/src/components/game/phantom/direBall.tsx</files>
  <action>
Refactor DireBalls container to use InstancedDireBalls component:

1. Import InstancedDireBalls:
   ```typescript
   import { InstancedDireBalls } from '../effects/instanced/InstancedDireBalls';
   ```

2. Replace current rendering logic:
   - Remove individual `<DireBall key={ball.id} {...ballProps} />` map
   - Replace with single `<InstancedDireBalls />` component

3. Update DireBalls return:
   ```tsx
   return (
     <>
       <InstancedDireBalls />
       {/* Individual particle trails for each ball (minimal overhead) */}
       {activeBalls.map((ball) => (
         <DireBallParticleTrail key={ball.id} ball={ball} />
       ))}
     </>
   );
   ```

4. Extract particle trail rendering:
   - Create new `DireBallParticleTrail` component (just the `<points>` element)
   - This keeps particles as individual elements (acceptable, only 1 per ball)
   - Removes collision detection logic from DireBall (handled by InstancedDireBalls)

5. Keep cleanup interval in useEffect:
   - `clearExpiredDireBalls()` every 500ms
   - Filter expired balls for rendering

6. Add comment explaining instancing:
   ```typescript
   // Instanced rendering: All dire balls render in single draw call via InstancedMesh
   // Each dire ball = 4 instances (core, outerGlow, innerBrightCore, secondaryGlow)
   // Particle trails render individually (minimal overhead: 1 points per ball)
   // Position/rotation updates at 60fps without React re-renders
   ```

7. DO NOT delete DireBall component yet (kept for reference/fallback, now split into particle trail only)
  </action>
  <verify>
1. DireBalls imports InstancedDireBalls
2. Renders `<InstancedDireBalls />` instead of mapping full DireBall components
3. Particle trails still render (extracted to DireBallParticleTrail or kept as minimal component)
4. Cleanup interval still runs every 500ms
5. Comment explaining instancing added
  </verify>
  <done>
DireBalls container refactored to use InstancedDireBalls, all dire balls render via InstancedMesh in single draw call with particle trails as individual elements
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete instanced dire ball rendering system with InstancedDireBalls component integrated into DireBalls container</what-built>
  <how-to-verify>
1. Start the game and spawn dire balls (Phantom E ability)
2. Open r3f-perf monitor (top-left corner)
3. Fire 10+ dire balls simultaneously
4. Observe the "calls" metric in r3f-perf:
   - Before optimization: 40+ calls (4 meshes × 10+ balls)
   - After optimization: ~4 calls (one per ball part type: core, outerGlow, innerCore, secondaryGlow)
5. Verify dire balls render correctly:
   - Dark void core with swirling patterns visible
   - Cyan energy core visible in center
   - Outer glow with fresnel effect visible
   - Purple/violet color scheme intact
6. Verify shader animations work:
   - Pulse effect: outer glow intensity cycles
   - Swirl effect: inner core rotates with swirling noise
   - Lightning crackle: electrical discharge patterns
7. Check visual quality matches pre-optimization:
   - Colors unchanged (deep void, violet, cyan accent)
   - Size and proportions identical (BALL_RADIUS = 0.21)
   - Particles trail behind each ball
8. Test collision detection:
   - Balls disappear when hitting terrain
   - Balls damage NPCs on contact
   - Balls expire after 3 seconds

Type "approved" if instancing is working correctly, or describe issues to fix.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. r3f-perf shows ~80% reduction in draw calls during heavy dire ball use
2. All dire ball parts render correctly (core, outer glow, cyan center, secondary glow)
3. Shader animations work (pulse, swirl, lightning crackle, rotation)
4. Particle trails still render behind each ball
5. Collision detection still works (terrain, NPCs)
6. No visual artifacts (flickering, missing parts, incorrect colors)
7. Expired balls hidden properly (scale=0)
</verification>

<success_criteria>
1. 30 dire balls render in single draw call per ball part type (4 calls total)
2. r3f-perf shows 80%+ reduction in draw calls during dire ball spam
3. Visual quality identical to pre-optimization (shader effects, colors, size)
4. Collision detection and lifetime behavior unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-instanced-rendering/03-02-SUMMARY.md`
</output>
