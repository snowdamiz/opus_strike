---
phase: 03-instanced-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/components/game/blaze/rockets.tsx
  - apps/client/src/components/game/effects/instanced/InstancedRockets.tsx
autonomous: false
user_setup: []

must_haves:
  truths:
    - "30 rockets on screen render in single draw call (not 30+)"
    - "r3f-perf shows draw call reduction of 80%+ during heavy rocket use"
    - "Rocket visual quality identical to pre-optimization (no art regression)"
    - "Rocket positions update correctly based on velocity and gravity"
  artifacts:
    - path: "apps/client/src/components/game/effects/instanced/InstancedRockets.tsx"
      provides: "InstancedMesh-based rocket renderer"
      min_lines: 100
    - path: "apps/client/src/components/game/blaze/rockets.tsx"
      provides: "RocketsManager using instanced renderer"
      exports: ["RocketsManager"]
  key_links:
    - from: "apps/client/src/components/game/blaze/rockets.tsx"
      to: "apps/client/src/store/gameStore.ts"
      via: "useGameStore(state => state.rockets)"
      pattern: "useGameStore.*rockets"
    - from: "apps/client/src/components/game/effects/instanced/InstancedRockets.tsx"
      to: "@react-three/drei"
      via: "import { Instance, Instances } from '@react-three/drei'"
      pattern: "from '@react-three/drei'"
---

<objective>
Implement InstancedMesh for rockets using @react-three/drei's Instances API, reducing 30+ draw calls to a single draw call for all active rockets.

Purpose: Rockets currently render as individual mesh components (6 meshes per rocket × 30 max = 180+ draw calls). Instancing consolidates all rockets into a single GPU draw call while maintaining visual quality.
Output: New InstancedRockets component using Drei Instances API, integrated into RocketsManager
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-instanced-rendering/03-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md

# Current rocket rendering implementation
@apps/client/src/components/game/blaze/rockets.tsx

# Projectile type definitions
@apps/client/src/store/types.ts

# Shared geometries and materials
@apps/client/src/components/game/effectResources.ts
</context>

<tasks>

<task type="auto">
  <name>Create InstancedRockets component with Drei Instances API</name>
  <files>apps/client/src/components/game/effects/instanced/InstancedRockets.tsx</files>
  <action>
Create new instanced renderer for rockets using @react-three/drei's Instances API:

1. Import dependencies:
   - `Instance, Instances` from '@react-three/drei'
   - `useFrame` from '@react-three/fiber'
   - `useGameStore` for rocket data access
   - SHARED_GEOMETRIES from effectResources

2. Define interface for instance data:
   ```typescript
   interface RocketInstance {
     id: string;
     position: { x: number; y: number; z: number };
     velocity: { x: number; y: number; z: number };
     startTime: number;
     alive: boolean;
   }
   ```

3. Create InstancedRockets component:
   - Set `limit={MAX_ROCKETS}` (50 to account for multi-player scenarios)
   - Use `SHARED_GEOMETRIES.cone8` for all rocket instances (shared geometry)
   - Create materials using existing `getRocketBodyMaterial()`, `getRocketNoseMaterial()`, etc.
   - Render each rocket as 6 instances (body, nose, fireCore, fireInner, fireOuter, smoke)

4. Position update logic in useFrame:
   - Access rockets non-reactively: `useGameStore.getState().rockets`
   - Calculate position with gravity: `y = initialY + velocity.y * elapsed - elapsed^2`
   - Calculate rotation to face velocity direction
   - Set instance `scale={alive ? 1 : 0}` to hide dead rockets

5. DO NOT use React state for instance visibility (causes re-renders)
   - Use Instance scale prop (1 = visible, 0 = hidden)
   - Position updates happen in useFrame, not via setState

6. Return Instances component with:
   - Shared geometries as children (once, not per-instance)
   - Instance elements for each rocket part (body, nose, fire effects)
   - Key prop for stable reconciliation

File location: Create new directory `apps/client/src/components/game/effects/instanced/`
  </action>
  <verify>
1. File exists at `apps/client/src/components/game/effects/instanced/InstancedRockets.tsx`
2. Component exports `InstancedRockets` function
3. Uses `Instances` and `Instance` from '@react-three/drei'
4. Accesses rocket data via `useGameStore.getState()` (non-reactive)
5. Has `MAX_ROCKETS = 50` constant
6. Updates instance positions in useFrame hook
  </verify>
  <done>
InstancedRockets component created with Drei Instances API, rendering all rockets in single draw call with position/rotation updates in useFrame
  </done>
</task>

<task type="auto">
  <name>Integrate InstancedRockets into RocketsManager</name>
  <files>apps/client/src/components/game/blaze/rockets.tsx</files>
  <action>
Refactor RocketsManager to use InstancedRockets component:

1. Import InstancedRockets:
   ```typescript
   import { InstancedRockets } from '../effects/instanced/InstancedRockets';
   ```

2. Replace current rendering logic:
   - Remove individual `<RocketEffect key={rocket.id} rocket={rocket} />` map
   - Replace with single `<InstancedRockets />` component
   - Keep the shared PointLight for ambient glow effect

3. Update RocketsManager return:
   ```tsx
   return (
     <group>
       <InstancedRockets />
       <pointLight ref={lightRef} color={0xff6600} intensity={0} distance={12} decay={2} />
     </group>
   );
   ```

4. Keep existing light update logic in useFrame:
   - Calculate average rocket position for light placement
   - Update light intensity based on active rocket count

5. DO NOT delete RocketEffect component yet (kept for reference/fallback)

6. Add comment explaining instancing:
   ```typescript
   // Instanced rendering: All rockets render in single draw call via InstancedMesh
   // Each rocket = 6 instances (body, nose, fireCore, fireInner, fireOuter, smoke)
   // Position/rotation updates at 60fps without React re-renders
   ```

7. Remove `MAX_ROCKETS` slice logic (`.slice(0, MAX_ROCKETS)`) since InstancedRockets handles limit internally
  </action>
  <verify>
1. RocketsManager imports InstancedRockets
2. Renders `<InstancedRockets />` instead of mapping RocketEffect components
3. Shared PointLight still renders and updates correctly
4. No `.slice()` on rockets array
5. Comment explaining instancing added above InstancedRockets usage
  </verify>
  <done>
RocketsManager refactored to use InstancedRockets, all rockets render via InstancedMesh in single draw call
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete instanced rocket rendering system with InstancedRockets component integrated into RocketsManager</what-built>
  <how-to-verify>
1. Start the game and spawn rockets (Blaze Q ability)
2. Open r3f-perf monitor (top-left corner)
3. Fire 10+ rockets simultaneously
4. Observe the "calls" metric in r3f-perf:
   - Before optimization: 60+ calls (6 meshes × 10+ rockets)
   - After optimization: ~6 calls (one per rocket part type: body, nose, 3 fire parts, smoke)
5. Verify rockets render correctly:
   - Rocket body, nose, fire trail, and smoke all visible
   - Rockets follow expected trajectory with gravity
   - Rockets rotate to face velocity direction
   - Expired rockets disappear (scale=0)
6. Check visual quality matches pre-optimization:
   - Colors unchanged (orange body, glowing nose, fire trail)
   - Size and proportions identical
   - Fire effect animation works

Type "approved" if instancing is working correctly, or describe issues to fix.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. r3f-perf shows ~80% reduction in draw calls during heavy rocket use
2. All rocket parts render correctly (body, nose, 3 fire layers, smoke)
3. Rocket positions animate correctly with gravity and velocity
4. No visual artifacts (flickering, missing parts, incorrect scale)
5. Expired rockets hidden properly (scale=0)
6. Performance monitored with r3f-perf shows "calls" metric reduced
</verification>

<success_criteria>
1. 30 rockets render in single draw call per rocket part type (6 calls total)
2. r3f-perf shows 80%+ reduction in draw calls during rocket spam
3. Visual quality identical to pre-optimization (no art regression)
4. Rockets follow correct physics trajectory (gravity, velocity)
</success_criteria>

<output>
After completion, create `.planning/phases/03-instanced-rendering/03-01-SUMMARY.md`
</output>
