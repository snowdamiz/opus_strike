---
phase: 02-team-base-construction
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - apps/client/src/components/game/maps/sci-fi-ctf/colliders/MapColliders.ts
autonomous: false

must_haves:
  truths:
    - "Players spawn at correct positions within team bases"
    - "Flag can be picked up at configured flag zone positions"
    - "Spawn indicators are visible at spawn positions"
    - "Flag zones pulse faster when enemy approaches"
  artifacts:
    - path: "apps/client/src/components/game/maps/sci-fi-ctf/colliders/MapColliders.ts"
      provides: "Physics colliders covering spawn platform areas"
      contains: "spawn"
  key_links:
    - from: "MatchManager"
      to: "SpawnManager"
      via: "setSpawnPoints"
      pattern: "setSpawnPoints"
    - from: "FlagZone"
      to: "gameStore"
      via: "useGameStore"
      pattern: "useGameStore"
---

<objective>
Verify integration of spawn points and flag zones with game systems, and ensure spawn positions have proper floor colliders.

Purpose: Ensure all Phase 2 work integrates correctly - spawns work with SpawnManager, flags work with FlagManager, visuals match game state.

Output: Verified working spawn/flag system with any necessary collider updates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-team-base-construction/02-RESEARCH.md

# Prior plan summaries
@.planning/phases/02-team-base-construction/02-01-SUMMARY.md
@.planning/phases/02-team-base-construction/02-02-SUMMARY.md
@.planning/phases/02-team-base-construction/02-03-SUMMARY.md
@.planning/phases/02-team-base-construction/02-04-SUMMARY.md

# Colliders file for any updates needed
@apps/client/src/components/game/maps/sci-fi-ctf/colliders/MapColliders.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify spawn position collider coverage</name>
  <files>apps/client/src/components/game/maps/sci-fi-ctf/colliders/MapColliders.ts</files>
  <action>
Review MapColliders.ts to ensure all 5 spawn positions per team have floor colliders underneath.

Spawn positions from config:
- Team A: (-80, 1, -12), (-80, 1, 0), (-80, 1, 12), (-75, 1, -6), (-75, 1, 6)
- Team B: (80, 1, -12), (80, 1, 0), (80, 1, 12), (75, 1, -6), (75, 1, 6)

Current colliders should cover:
- Team A main platform at base x=-80, spanning 30x40 area (-95 to -65, -20 to 20)
- Team B main platform at base x=80, spanning similar area (65 to 95, -20 to 20)

Check if spawn positions fall within existing floor colliders. All spawns should be covered by the main spawn platform colliders created in Phase 1.

If any spawn positions are outside existing colliders, add floor colliders. The spawn Y=1 means players spawn 1 unit above floor. Floor collider should be at y=0.

Likely outcome: Existing colliders should cover all spawns since they're within the 30x40 base platform area. Just verify and document.

If spawns at z=-12 or z=12 fall outside the 40-unit depth (z=-20 to z=20), add extended floor colliders.
  </action>
  <verify>
Check collider coverage by reviewing positions in MapColliders.ts match spawn areas.
  </verify>
  <done>
All spawn positions have floor colliders beneath them.
Players will not fall through when spawning.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build and start dev server</name>
  <files></files>
  <action>
Build all packages and start the development server to prepare for visual verification.

1. Full monorepo build:
```bash
pnpm build
```

2. Start development server:
```bash
cd apps/client && pnpm dev
```

Document any build errors. If errors occur related to the Phase 2 changes, diagnose and fix before proceeding to verification.

Common issues to check:
- Missing imports (SpawnIndicator, FlagZone)
- Type mismatches with Vec3
- Circular dependencies
  </action>
  <verify>
`pnpm build` completes without errors.
Dev server starts and game loads in browser.
  </verify>
  <done>
Full monorepo builds successfully.
Development server starts without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete spawn and flag zone integration for Phase 2:
1. Spawn positions configured in MAP_CONFIG (5 per team)
2. Flag positions configured in MAP_CONFIG
3. MatchManager uses configured positions
4. Spawn indicators visible at spawn points
5. Flag zone indicators with contested-state effects
  </what-built>
  <how-to-verify>
1. Start the game: Open browser to http://localhost:5173
2. Join a match (or start single-player test mode if available)
3. Observe spawn:
   - Player should spawn within their team's base area
   - Spawn indicator rings should be visible on the floor
4. Navigate to flag zones:
   - Go to your team's flag zone (back of base)
   - Flag zone boundary markers should be visible
   - Zone should have gentle pulse animation
5. Test contested state:
   - If multiplayer: Have enemy player approach flag zone
   - Zone should pulse noticeably faster when enemy nearby
6. Test flag interaction:
   - Walk to enemy flag
   - Flag should be pickable at the configured position
   - Flag position should match the visual flag zone indicator

Expected positions:
- Team A (red): Spawn around x=-80, flag at x=-90
- Team B (blue): Spawn around x=80, flag at x=92
  </how-to-verify>
  <resume-signal>
Type "verified" if spawn and flag systems work correctly.
Describe any issues found if verification fails.
  </resume-signal>
</task>

</tasks>

<verification>
1. All packages build: `pnpm build`
2. Spawn positions fall within floor collider coverage
3. Visual verification confirms:
   - Spawn indicators visible
   - Flag zones visible with pulse
   - Players spawn in correct locations
   - Flag interaction works at configured positions
</verification>

<success_criteria>
1. Monorepo builds without errors
2. All spawn positions have floor colliders
3. User verifies spawn indicators are visible
4. User verifies flag zones have contested-state pulse
5. User verifies players spawn at correct base positions
6. User verifies flags can be picked up at configured positions
</success_criteria>

<output>
After completion, create `.planning/phases/02-team-base-construction/02-05-SUMMARY.md`
</output>
