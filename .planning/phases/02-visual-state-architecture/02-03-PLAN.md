---
phase: 02-visual-state-architecture
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - apps/client/src/components/game/PerfMonitor.tsx
  - apps/client/src/components/game/GameCanvas.tsx
  - package.json
autonomous: true
user_setup:
  - service: npm
    why: "Install r3f-perf package for performance monitoring"
    env_vars: []
    actions:
      - task: "Install r3f-perf as dev dependency"
        command: "npm install --save-dev r3f-perf"

must_haves:
  truths:
    - "r3f-perf package is installed in package.json"
    - "PerfMonitor component renders r3f-perf's Perf component in Canvas"
    - "Real-time FPS and GPU metrics are visible during gameplay"
    - "Performance monitor shows FPS, GPU time, triangle count"
    - "Monitor is positioned in corner (top-left or similar)"
  artifacts:
    - path: "apps/client/src/components/game/PerfMonitor.tsx"
      provides: "Performance monitoring component using r3f-perf"
      min_lines: 15
    - path: "apps/client/src/components/game/GameCanvas.tsx"
      contains: "PerfMonitor"
    - path: "package.json"
      contains: "r3f-perf"
  key_links:
    - from: "PerfMonitor.tsx"
      to: "r3f-perf"
      via: "import { Perf } from 'r3f-perf'"
      pattern: "from 'r3f-perf'"
    - from: "GameCanvas.tsx"
      to: "PerfMonitor"
      via: "JSX <PerfMonitor /> component"
      pattern: "<PerfMonitor"
---

<objective>
Install r3f-perf and add performance monitoring component to display real-time FPS, GPU time, and rendering metrics during gameplay.

Purpose: Enable performance profiling to measure the impact of optimizations. r3f-perf provides React Three Fiber-specific metrics (GPU calls, triangle count, geometries) that browser DevTools don't show.

Output: PerfMonitor component integrated in GameCanvas, showing real-time performance metrics during gameplay.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-state-architecture/02-RESEARCH.md

@.planning/phases/02-visual-state-architecture/02-01-SUMMARY.md
@.planning/phases/02-visual-state-architecture/02-02-SUMMARY.md

@apps/client/src/components/game/GameCanvas.tsx
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install r3f-perf package</name>
  <files>package.json</files>
  <action>
Run npm install to add r3f-perf as a dev dependency:

```bash
npm install --save-dev r3f-perf
```

This adds the performance monitoring library specifically designed for React Three Fiber applications. Confirm installation by checking that r3f-perf appears in package.json devDependencies.
  </action>
  <verify>grep -q "r3f-perf" /Users/sn0w/Documents/dev/opus_strike/package.json || grep -q "r3f-perf" /Users/sn0w/Documents/dev/opus_strike/apps/client/package.json</verify>
  <done>r3f-perf is installed in package.json devDependencies, available for import</done>
</task>

<task type="auto">
  <name>Task 2: Create PerfMonitor component</name>
  <files>apps/client/src/components/game/PerfMonitor.tsx</files>
  <action>
Create `apps/client/src/components/game/PerfMonitor.tsx`:

1. Import Perf from r3f-perf: `import { Perf } from 'r3f-perf';`

2. Create and export a functional component:
   ```typescript
   export function PerfMonitor() {
     return (
       <Perf
         position="top-left"
         minimal={false}
         styleChartSize={200}
       />
     );
   }
   ```

3. Props explanation:
   - `position="top-left"` - Places monitor in top-left corner of canvas
   - `minimal={false}` - Show detailed metrics (FPS, GPU, triangles, geometries, etc.)
   - `styleChartSize={200}` - Size of the performance charts in pixels

4. Export as default: `export default PerfMonitor;`

This component can now be dropped into any React Three Fiber Canvas.
  </action>
  <verify>grep -q "from 'r3f-perf'" apps/client/src/components/game/PerfMonitor.tsx && grep -q "<Perf" apps/client/src/components/game/PerfMonitor.tsx</verify>
  <done>PerfMonitor.tsx created with r3f-perf integration, exports PerfMonitor component</done>
</task>

<task type="auto">
  <name>Task 3: Add PerfMonitor to GameCanvas</name>
  <files>apps/client/src/components/game/GameCanvas.tsx</files>
  <action>
Modify `apps/client/src/components/game/GameCanvas.tsx`:

1. Add import: `import { PerfMonitor } from './PerfMonitor';`

2. Add PerfMonitor component inside the Canvas, after the lighting setup and before the game objects (around line 60):
   ```typescript
   <Canvas ...>
     <Suspense fallback={null}>
       {/* Lighting */}
       ...

       {/* Performance monitor */}
       <PerfMonitor />

       {/* World */}
       <VoxelWorld />
       ...
     </Suspense>
   </Canvas>
   ```

3. Place PerfMonitor early in the component tree so it's always rendered (not conditionally based on game phase)

4. The monitor will show real-time FPS, GPU time, triangle count, and other R3F-specific metrics during gameplay
  </action>
  <verify>grep -q "import.*PerfMonitor" apps/client/src/components/game/GameCanvas.tsx && grep -q "<PerfMonitor" apps/client/src/components/game/GameCanvas.tsx</verify>
  <done>PerfMonitor is rendered in GameCanvas, performance metrics visible during gameplay</done>
</task>

</tasks>

<verification>
After completion:
1. r3f-perf is installed in package.json
2. PerfMonitor.tsx component exists and uses r3f-perf's Perf component
3. PerfMonitor is imported and rendered in GameCanvas
4. TypeScript compilation passes
5. Performance metrics are visible in top-left corner during gameplay
6. Metrics include FPS, GPU time, triangle count, geometries
</verification>

<success_criteria>
1. r3f-perf installed successfully
2. PerfMonitor component created
3. PerfMonitor rendered in GameCanvas
4. Real-time performance metrics visible during gameplay
5. No TypeScript errors
6. FPS, GPU time, and triangle count displayed
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-state-architecture/02-03-SUMMARY.md`
</output>
