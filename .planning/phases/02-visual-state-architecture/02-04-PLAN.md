---
phase: 02-visual-state-architecture
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/components/game/PlayerController.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Local player position updates ONLY go to visualStore, not gameStore"
    - "gameStore.updateLocalPlayer() is no longer called with position/velocity/rotation data every frame"
    - "Local player movement does not trigger React re-renders via gameStore.players Map updates"
  artifacts:
    - path: "apps/client/src/components/game/PlayerController.tsx"
      provides: "Local player controller with visual-only position updates"
      min_lines: 600
    - path: "apps/client/src/store/gameStore.ts"
      provides: "Game state management without per-frame position updates"
      contains: "updateLocalPlayer"
  key_links:
    - from: "PlayerController.tsx"
      to: "visualStore"
      via: "setPlayerVisualPosition/setPlayerVisualRotation calls only"
      pattern: "setPlayerVisual(Position|Rotation)"
    - from: "PlayerController.tsx"
      to: "gameStore.updateLocalPlayer"
      via: "No position/velocity/rotation data in call"
      pattern: "updateLocalPlayer.*movement.*isGrounded"
---

<objective>
Remove per-frame position/velocity/rotation updates from gameStore.updateLocalPlayer() call to eliminate React re-renders from local player movement.

Purpose: Local player position currently updates gameStore every frame (line 519 in PlayerController.tsx), which mutates the gameStore.players Map and triggers React re-renders for all subscribed components. Position data should ONLY flow to visualStore (non-reactive), while gameStore should only update on game events (abilities, damage, flag capture, etc.).

Output: Local player movement updates visualStore only, gameStore.updateLocalPlayer() called only for game event state changes (movement flags, ability state, etc.).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-visual-state-architecture/02-visual-state-architecture-VERIFICATION.md
@.planning/phases/02-visual-state-architecture/02-01-SUMMARY.md
@.planning/phases/02-visual-state-architecture/02-02-SUMMARY.md
@.planning/phases/02-visual-state-architecture/02-03-SUMMARY.md

# Gap being addressed
Gap from VERIFICATION.md lines 7-17:
- truth: "Player position interpolation updates at 60 FPS without triggering store updates"
- status: partial
- reason: "Local player position still updates gameStore every frame via updateLocalPlayer(), causing unnecessary re-renders"
- missing: "Local player position updates should ONLY go to visualStore, not gameStore"

# Current implementation context
From PlayerController.tsx line 519-532:
```typescript
updateLocalPlayer({
  position: { x: position.x, y: position.y, z: position.z },
  velocity: { x: velocity.x, y: velocity.y, z: velocity.z },
  lookYaw: cameraControl.refs.yaw.current,
  lookPitch: cameraControl.refs.pitch.current,
  movement: {
    ...localPlayer.movement,
    isGrounded: movement.refs.isGrounded.current,
    isSprinting: movement.refs.isSprinting.current,
    isCrouching: movement.refs.isCrouching.current,
    isSliding,
    slideTimeRemaining: movement.refs.slideTime.current,
  },
});
```

This is called EVERY FRAME in useFrame, updating position/velocity/rotation which causes gameStore.players Map to update and trigger React re-renders.

The visualStore is already being updated correctly at lines 535-536:
```typescript
setPlayerVisualPosition(localPlayer.id, { x: position.x, y: position.y, z: position.z });
setPlayerVisualRotation(localPlayer.id, cameraControl.refs.yaw.current);
```

# What needs to change
1. Remove position, velocity, lookYaw, lookPitch from the updateLocalPlayer() call
2. Keep only movement state updates (isGrounded, isSprinting, isCrouching, isSliding, slideTimeRemaining)
3. Position/velocity/rotation already flow to visualStore (lines 535-536) - this is the ONLY path for visual position data
4. Game event state changes (abilities, damage, etc.) can still use updateLocalPlayer() as needed

Reference: 02-02-SUMMARY.md established that visualStore is the canonical source for 60fps position interpolation.
</context>

<tasks>

<task type="auto">
  <name>Remove position/velocity/rotation from per-frame updateLocalPlayer call</name>
  <files>apps/client/src/components/game/PlayerController.tsx</files>
  <action>
In PlayerController.tsx, modify the updateLocalPlayer() call at line 519-532:

1. Remove these fields from the updateLocalPlayer() call:
   - position
   - velocity
   - lookYaw
   - lookPitch

2. Keep only the movement state updates:
   ```typescript
   updateLocalPlayer({
     movement: {
       ...localPlayer.movement,
       isGrounded: movement.refs.isGrounded.current,
       isSprinting: movement.refs.isSprinting.current,
       isCrouching: movement.refs.isCrouching.current,
       isSliding,
       slideTimeRemaining: movement.refs.slideTime.current,
     },
   });
   ```

3. Add a comment explaining this is intentional - position data flows to visualStore only (non-reactive), while gameStore tracks only game event state (movement flags, abilities, etc.)

4. Verify that setPlayerVisualPosition() and setPlayerVisualRotation() calls at lines 535-536 remain unchanged - these are the ONLY path for local player position data now.

WHY: The visualStore is already receiving position/rotation updates at 60fps (lines 535-536). Updating gameStore with the same data every frame causes unnecessary React re-renders because it mutates the players Map reference. Game state should only track game events (abilities fired, player hit, flag capture), not per-frame position data.
  </action>
  <verify>
1. TypeScript compilation passes: npx tsc --noEmit -p apps/client/tsconfig.json
2. Search for updateLocalPlayer calls to verify position/velocity/lookYaw/lookPitch are NOT in the per-frame call:
   grep -A 10 "updateLocalPlayer" apps/client/src/components/game/PlayerController.tsx
3. Verify visualStore calls are still present:
   grep "setPlayerVisual" apps/client/src/components/game/PlayerController.tsx
  </verify>
  <done>
- updateLocalPlayer() in useFrame no longer includes position/velocity/lookYaw/lookPitch fields
- Only movement state (isGrounded, isSprinting, isCrouching, isSliding, slideTimeRemaining) updates every frame
- setPlayerVisualPosition() and setPlayerVisualRotation() remain active (60fps visual updates)
- Comment in code explains the separation: visualStore for position, gameStore for game events
  </done>
</task>

<task type="auto">
  <name>Verify local player movement doesn't trigger gameStore re-renders</name>
  <files>apps/client/src/components/game/PlayerController.tsx, apps/client/src/store/gameStore.ts</files>
  <action>
Verify the fix by checking:

1. In PlayerController.tsx, confirm updateLocalPlayer() is NOT called with position data:
   - The per-frame updateLocalPlayer call (around line 519) should only update movement state
   - No position, velocity, lookYaw, or lookPitch fields in the call

2. In gameStore.ts, review updateLocalPlayer() implementation (lines 288-300):
   - Confirm it creates a new Map only when called
   - With position removed from the call, this should now only update movement flags

3. Check for any other locations where updateLocalPlayer() is called with position data:
   - Search for all updateLocalPlayer calls in PlayerController.tsx
   - Any calls outside the per-frame useFrame loop (e.g., initialization, ability effects) should still work as expected

4. Verify visualStore integration:
   - setPlayerVisualPosition is called every frame with position data
   - setPlayerVisualRotation is called every frame with rotation data
   - These are the ONLY path for local player position/rotation to reach rendering

Expected outcome: Local player position updates flow ONLY to visualStore (non-reactive), while gameStore.updateLocalPlayer() only tracks movement state flags (isGrounded, isSliding, etc.). This eliminates React re-renders from local player movement.
  </action>
  <verify>
1. TypeScript compilation passes
2. grep -n "updateLocalPlayer" apps/client/src/components/game/PlayerController.tsx shows only movement state updates in useFrame
3. grep -n "setPlayerVisual" apps/client/src/components/game/PlayerController.tsx shows position/rotation updates are present
4. Read the updateLocalPlayer call in useFrame to confirm no position/velocity/rotation fields
  </verify>
  <done>
- Local player position updates verified to ONLY go to visualStore (setPlayerVisualPosition/Rotation)
- gameStore.updateLocalPlayer() confirmed to only update movement state flags
- No other code paths are updating gameStore with per-frame position data
- React DevTools profiler should show OtherPlayers does NOT re-render during local player movement
  </done>
</task>

</tasks>

<verification>
After completing these tasks, verify:

1. **TypeScript compilation**: npx tsc --noEmit -p apps/client/tsconfig.json
2. **Code inspection**: updateLocalPlayer() call in useFrame does not include position/velocity/rotation fields
3. **VisualStore active**: setPlayerVisualPosition/Rotation calls are still present every frame
4. **No regressions**: Other calls to updateLocalPlayer() (initialization, abilities) still work correctly

The verification will confirm that:
- Local player position data flows ONLY to visualStore (non-reactive, 60fps)
- gameStore.updateLocalPlayer() only tracks movement state flags (game events)
- React re-renders are eliminated from local player movement
</verification>

<success_criteria>
- [ ] updateLocalPlayer() in PlayerController useFrame no longer includes position/velocity/lookYaw/lookPitch
- [ ] Only movement state fields (isGrounded, isSprinting, isCrouching, isSliding, slideTimeRemaining) are updated every frame
- [ ] setPlayerVisualPosition() and setPlayerVisualRotation() remain active every frame
- [ ] Comment added explaining the separation of visual and game state
- [ ] TypeScript compilation passes
- [ ] No regressions in other updateLocalPlayer() call sites
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-state-architecture/02-04-SUMMARY.md`
</output>
