---
phase: 01-map-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/components/game/VoxelWorld.tsx
  - apps/client/src/hooks/usePhysics.ts
  - apps/client/src/components/game/maps/sci-fi-ctf/index.ts
  - apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx
  - apps/client/src/components/game/maps/sci-fi-ctf/materials.ts
  - apps/client/src/components/game/maps/sci-fi-ctf/config.ts
autonomous: true

must_haves:
  truths:
    - "Old GLB map no longer renders in game"
    - "New map component scaffolding exists and renders empty scene"
    - "Shared materials defined for sci-fi aesthetic (dark base, team accents, glow)"
  artifacts:
    - path: "apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx"
      provides: "Main map component"
      exports: ["SciFiCTFMap"]
    - path: "apps/client/src/components/game/maps/sci-fi-ctf/materials.ts"
      provides: "Shared material instances"
      exports: ["floorMaterial", "teamAAccent", "teamBAccent", "hazardMaterial", "wallMaterial", "glowMaterial"]
    - path: "apps/client/src/components/game/maps/sci-fi-ctf/config.ts"
      provides: "Map dimensions and spawn positions"
      exports: ["MAP_CONFIG"]
  key_links:
    - from: "apps/client/src/components/game/VoxelWorld.tsx"
      to: "apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx"
      via: "import and render"
      pattern: "import.*SciFiCTFMap"
---

<objective>
Remove the old imported GLB map and scaffold the new procedural sci-fi CTF map structure.

Purpose: Establish the foundation for the new map by removing the old dependency and creating the component structure with shared materials that all subsequent plans will use.

Output: Empty map component rendering in place of old GLB, with materials and config ready for geometry.
</objective>

<execution_context>
@~/.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-map-foundation/01-CONTEXT.md
@.planning/phases/01-map-foundation/01-RESEARCH.md
@apps/client/src/components/game/VoxelWorld.tsx
@apps/client/src/hooks/usePhysics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove GLB map and scaffold new map structure</name>
  <files>
    apps/client/src/components/game/VoxelWorld.tsx
    apps/client/src/hooks/usePhysics.ts
    apps/client/src/components/game/maps/sci-fi-ctf/index.ts
    apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx
    apps/client/src/components/game/maps/sci-fi-ctf/config.ts
  </files>
  <action>
    1. Create the maps directory structure:
       - `apps/client/src/components/game/maps/sci-fi-ctf/`

    2. Create `config.ts` with map configuration:
       - MAP_DIMENSIONS: { width: 200, depth: 100 } (elongated rectangle)
       - TEAM_A_BASE: { x: -80, y: 0, z: 0 } (left side - tech/platform)
       - TEAM_B_BASE: { x: 80, y: 0, z: 0 } (right side - natural/cave)
       - GROUND_Y: 0 (base ground level)
       - PLATFORM_HEIGHT: 3 (standard platform elevation)
       - WALL_HEIGHT: 15 (boundary wall height)

    3. Create `SciFiCTFMap.tsx`:
       - Export SciFiCTFMap component
       - Return a group with placeholder comment for geometry
       - Add a simple debug ground plane (100x200) at y=0 using Grid from drei
       - The Grid provides sci-fi floor aesthetic with fade

    4. Create `index.ts` barrel export

    5. Update `VoxelWorld.tsx`:
       - Remove GLBMap component entirely
       - Remove useGLTF import and preload
       - Import and render SciFiCTFMap instead
       - Keep Ground (fallback floor) and ArenaBoundaries components for now

    6. Update `usePhysics.ts`:
       - Remove CURRENT_MAP constant
       - Remove loadMapColliders function call from setup()
       - Remove GLTFLoader import
       - Keep the ground collider at y=-50 (safety net)
       - Keep boundary colliders for now (will update in Plan 05)
       - The map will have no collision until Plan 05 creates procedural colliders
  </action>
  <verify>
    - Run `pnpm build --filter @voxel-strike/client` - should compile without errors
    - Game should render without the old GLB map
    - Grid floor should be visible at ground level
  </verify>
  <done>
    Old GLB map removed, new SciFiCTFMap renders with Grid floor placeholder
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shared materials for sci-fi aesthetic</name>
  <files>
    apps/client/src/components/game/maps/sci-fi-ctf/materials.ts
  </files>
  <action>
    Create `materials.ts` with all shared MeshStandardMaterial instances:

    1. Base materials (dark colors):
       - `floorMaterial`: color #1a1a2e, metalness 0.8, roughness 0.3
       - `wallMaterial`: color #0d0d1a, metalness 0.7, roughness 0.4
       - `platformMaterial`: color #1f1f35, metalness 0.85, roughness 0.25

    2. Team A accents (red/orange warm):
       - `teamAAccent`: color #2a1a1a, emissive #ff4400, emissiveIntensity 0.5
       - `teamAGlow`: color #1a0a0a, emissive #ff6600, emissiveIntensity 1.2

    3. Team B accents (blue/cyan cool):
       - `teamBAccent`: color #1a1a2a, emissive #00ffff, emissiveIntensity 0.5
       - `teamBGlow`: color #0a0a1a, emissive #00ccff, emissiveIntensity 1.2

    4. Special materials:
       - `hazardMaterial`: color #1a0a1a, emissive #ff00ff, emissiveIntensity 0.8
       - `caveMaterial`: color #1a1512, metalness 0.3, roughness 0.8 (natural stone)
       - `energyBarrierMaterial`: color #000000, emissive #00ff88, emissiveIntensity 1.5, transparent true, opacity 0.6

    Export all materials as named exports for import by geometry components.

    IMPORTANT: Create materials using `new THREE.MeshStandardMaterial({...})` NOT inline JSX.
    This ensures single GPU resource per material type.
  </action>
  <verify>
    - File should export 10 material instances
    - TypeScript should compile without errors
    - Materials use THREE.MeshStandardMaterial constructor (not JSX)
  </verify>
  <done>
    All shared materials defined and exported for use by geometry components
  </done>
</task>

</tasks>

<verification>
- `pnpm build --filter @voxel-strike/client` compiles successfully
- Old GLB map no longer appears when running game
- New SciFiCTFMap component renders with Grid floor
- Materials file exports all required material instances
</verification>

<success_criteria>
1. VoxelWorld.tsx imports and renders SciFiCTFMap (not GLBMap)
2. usePhysics.ts no longer loads GLB colliders
3. materials.ts exports 10+ shared material instances
4. config.ts exports MAP_CONFIG with dimensions and base positions
5. Game runs without errors (no map geometry yet, just Grid floor)
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-foundation/01-01-SUMMARY.md`
</output>
