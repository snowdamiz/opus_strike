---
phase: 01-map-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/client/src/components/game/maps/sci-fi-ctf/geometry/CenterZone.tsx
  - apps/client/src/components/game/maps/sci-fi-ctf/geometry/Routes.tsx
  - apps/client/src/components/game/maps/sci-fi-ctf/geometry/index.ts
  - apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx
autonomous: true

must_haves:
  truths:
    - "Three distinct routes connect Team A and Team B bases"
    - "Routes have different engagement characteristics (close, medium, long)"
    - "Mid-map has interconnection passages between routes"
    - "Hazard zones exist at strategic positions"
  artifacts:
    - path: "apps/client/src/components/game/maps/sci-fi-ctf/geometry/CenterZone.tsx"
      provides: "Central map area geometry"
      exports: ["CenterZone"]
    - path: "apps/client/src/components/game/maps/sci-fi-ctf/geometry/Routes.tsx"
      provides: "Three route corridors"
      exports: ["Routes"]
  key_links:
    - from: "apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx"
      to: "apps/client/src/components/game/maps/sci-fi-ctf/geometry/CenterZone.tsx"
      via: "import and render"
      pattern: "<CenterZone"
    - from: "apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx"
      to: "apps/client/src/components/game/maps/sci-fi-ctf/geometry/Routes.tsx"
      via: "import and render"
      pattern: "<Routes"
---

<objective>
Create the center zone and three connecting routes between team bases.

Purpose: Establish the main playable arena with three routes of different engagement distances (close-quarters, medium-range, long-range), interconnection passages for flanking, and hazard zones for risk/reward positioning.

Output: CenterZone and Routes components creating the full navigable path structure between bases.
</objective>

<execution_context>
@~/.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-map-foundation/01-CONTEXT.md
@.planning/phases/01-map-foundation/01-RESEARCH.md
@apps/client/src/components/game/maps/sci-fi-ctf/materials.ts
@apps/client/src/components/game/maps/sci-fi-ctf/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three routes with distinct characteristics</name>
  <files>
    apps/client/src/components/game/maps/sci-fi-ctf/geometry/Routes.tsx
  </files>
  <action>
    Create `geometry/Routes.tsx` with three distinct routes:

    **Route Layout (elongated map, teams on left/right):**
    - North route (z = -30): Long-range elevated
    - Middle route (z = 0): Medium-range mixed
    - South route (z = +30): Close-quarters tunnels

    **1. North Route (Long-range elevated):**
    - Elevated platform corridor at y=3
    - Width ~12 units, spanning from Team A to Team B
    - Open sightlines (few obstacles)
    - Exposed position (can be shot from below)
    - Use platformMaterial for floor
    - Add glow strip edges (energy barriers aesthetic)
    - Railings or low walls (y=0.5 height) for partial cover

    **2. Middle Route (Medium-range mixed):**
    - Ground level (y=0)
    - Width ~15 units
    - Mix of open areas and cover spots
    - Floor uses floorMaterial
    - Add scattered cover blocks (low walls, 1.5m height)
    - This is the "main street" - widest and most direct

    **3. South Route (Close-quarters tunnels):**
    - Ground level with tunnel sections
    - Width ~8 units (narrow)
    - Tight turns and corners
    - Low ceiling sections (tunnel aesthetic)
    - Use floorMaterial + wallMaterial for tunnel walls
    - Limited escape routes (committed engagement)
    - Add glow strips inside tunnels for visibility

    **Route floors span full length:**
    Each route is ~160 units long (from Team A base at x=-80 to Team B base at x=+80).
    Leave gaps where routes meet base areas (bases provide their own geometry).

    Component structure:
    ```tsx
    export function Routes() {
      return (
        <group>
          <NorthRoute />
          <MiddleRoute />
          <SouthRoute />
        </group>
      );
    }

    function NorthRoute() { /* Elevated platform corridor */ }
    function MiddleRoute() { /* Ground level with cover */ }
    function SouthRoute() { /* Tunnel system */ }
    ```
  </action>
  <verify>
    - `pnpm build --filter @voxel-strike/client` compiles
    - Routes component exports correctly
  </verify>
  <done>
    Three distinct routes created: elevated (north), mixed (middle), tunnels (south)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create center zone with interconnects and hazards</name>
  <files>
    apps/client/src/components/game/maps/sci-fi-ctf/geometry/CenterZone.tsx
  </files>
  <action>
    Create `geometry/CenterZone.tsx` with mid-map features:

    **1. Central hub area (x=0, z=0):**
    - Circular or octagonal platform at y=0
    - Diameter ~25 units
    - Acts as crossroads where all routes converge
    - Use floorMaterial with energyBarrierMaterial accent ring

    **2. Route interconnection passages:**
    - North-to-Middle connector (around x=-20 and x=+20)
    - Middle-to-South connector (around x=-20 and x=+20)
    - Each connector is ~6 units wide
    - Ramps/stairs where elevation differs
    - Allows flanking between routes

    **3. Hazard zones (void pits):**
    - Two void pits flanking the central hub
    - Position at z=-15 and z=+15, x=0
    - Size ~10x8 each
    - Use hazardMaterial for edges (glowing warning)
    - Actual pit is just a visual gap (no floor)
    - Players falling in will hit the y=-50 death plane

    **4. Elevation ramps:**
    - Ramps connecting ground (y=0) to elevated north route (y=3)
    - One ramp on each side (Team A side, Team B side)
    - Ramp angle ~30 degrees
    - Width ~5 units

    Component structure:
    ```tsx
    export function CenterZone() {
      return (
        <group>
          {/* Central hub platform */}
          {/* Route connectors */}
          {/* Hazard zones with glow edges */}
          {/* Elevation ramps */}
        </group>
      );
    }
    ```

    IMPORTANT: Hazard zones are visual-only in this phase. Physics integration (sensor colliders) happens in Phase 5.
  </action>
  <verify>
    - `pnpm build --filter @voxel-strike/client` compiles
    - CenterZone component exports correctly
  </verify>
  <done>
    Center zone created with hub, connectors, hazard pits, and ramps
  </done>
</task>

<task type="auto">
  <name>Task 3: Integrate routes and center zone into map</name>
  <files>
    apps/client/src/components/game/maps/sci-fi-ctf/geometry/index.ts
    apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx
  </files>
  <action>
    1. Update geometry/index.ts:
       - Add Routes and CenterZone to barrel exports

    2. Update SciFiCTFMap.tsx:
       - Import Routes and CenterZone from ./geometry
       - Render both components
       - Remove or reduce the Grid floor (geometry now covers most of map)
       - Keep a small Grid visible in center hub area for sci-fi effect

    The complete map should now show:
    - Team A base (left, tech) - from Plan 02
    - Team B base (right, cave) - from Plan 03
    - Three routes connecting them
    - Center hub with interconnects
    - Hazard pits with glowing edges
  </action>
  <verify>
    - Run game and visually navigate:
      - Can walk from Team A area toward center
      - Three distinct paths visible (elevated north, ground middle, tunnels south)
      - Center hub visible at map center
      - Hazard pits visible with warning glow
      - Connector passages between routes exist
  </verify>
  <done>
    Full map layout visible with all routes and center zone integrated
  </done>
</task>

</tasks>

<verification>
- `pnpm build --filter @voxel-strike/client` compiles successfully
- Three routes visible: north (elevated), middle (ground), south (tunnels)
- Routes connect team base areas to center
- Center hub visible at x=0, z=0
- Interconnection passages allow moving between routes
- Hazard pits visible with glowing edges
- Elevation changes work (ramps connect levels)
</verification>

<success_criteria>
1. Routes component creates three distinct paths
2. CenterZone component creates hub, connectors, hazards
3. SciFiCTFMap integrates all geometry
4. Routes have different engagement characteristics:
   - North: open sightlines, elevated exposure
   - Middle: mixed cover, direct path
   - South: tight corners, committed fights
5. Mid-map interconnects allow flanking between routes
6. Hazard zones visually marked (physics in Phase 5)
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-foundation/01-04-SUMMARY.md`
</output>
