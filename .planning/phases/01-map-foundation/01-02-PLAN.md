---
phase: 01-map-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/client/src/components/game/maps/sci-fi-ctf/geometry/TeamABase.tsx
  - apps/client/src/components/game/maps/sci-fi-ctf/geometry/index.ts
  - apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx
autonomous: true

must_haves:
  truths:
    - "Team A side has distinct tech/platform visual identity"
    - "Team A base area has elevated platforms at multiple heights"
    - "Team A side has clear spawn area and flag zone position"
  artifacts:
    - path: "apps/client/src/components/game/maps/sci-fi-ctf/geometry/TeamABase.tsx"
      provides: "Team A base geometry"
      exports: ["TeamABase"]
    - path: "apps/client/src/components/game/maps/sci-fi-ctf/geometry/index.ts"
      provides: "Geometry barrel exports"
  key_links:
    - from: "apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx"
      to: "apps/client/src/components/game/maps/sci-fi-ctf/geometry/TeamABase.tsx"
      via: "import and render at TEAM_A_BASE position"
      pattern: "<TeamABase"
---

<objective>
Create Team A base geometry with tech/platform sci-fi aesthetic.

Purpose: Establish the left side of the map with a distinct high-tech visual identity featuring elevated platforms, glowing accents, and clear strategic positions.

Output: TeamABase component rendering platforms, floor, and structural elements with red/orange glowing accents.
</objective>

<execution_context>
@~/.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-map-foundation/01-CONTEXT.md
@.planning/phases/01-map-foundation/01-RESEARCH.md
@apps/client/src/components/game/maps/sci-fi-ctf/materials.ts
@apps/client/src/components/game/maps/sci-fi-ctf/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Team A base geometry</name>
  <files>
    apps/client/src/components/game/maps/sci-fi-ctf/geometry/TeamABase.tsx
    apps/client/src/components/game/maps/sci-fi-ctf/geometry/index.ts
  </files>
  <action>
    Create `geometry/TeamABase.tsx` with tech/platform aesthetic:

    1. Main base platform (spawn area):
       - Large platform at y=0, size ~30x40 (width x depth)
       - Use floorMaterial for surface
       - Add teamAAccent trim around edges (thin strips with glow)

    2. Elevated command platform:
       - Raised platform at y=3 (PLATFORM_HEIGHT), size ~15x15
       - Position behind spawn area (further from center)
       - Add stairs/ramp connecting to ground level
       - Use platformMaterial for surface

    3. Flag zone platform:
       - Distinct hexagonal or circular platform at y=1
       - Position at the back-center of base
       - Use teamAGlow material for the platform edge to mark it clearly
       - This is where the flag will be placed (center of this platform)

    4. Tech structures:
       - Add 2-3 tall thin pillars (decorative, not blocking) with glow strips
       - Add small floating platform elements using useFrame for subtle bob animation
       - Pillars positioned at corners of main platform

    5. Route connection points:
       - Three openings facing center: top (north), middle, bottom (south)
       - Each opening ~8-10 units wide
       - Mark openings with teamAGlow strips on floor

    Component structure:
    ```tsx
    export function TeamABase() {
      return (
        <group position={[TEAM_A_BASE.x, 0, TEAM_A_BASE.z]}>
          {/* Main spawn platform */}
          {/* Elevated command platform */}
          {/* Flag zone */}
          {/* Decorative pillars */}
          {/* Route markers */}
        </group>
      );
    }
    ```

    Use boxGeometry for all shapes. Import materials from ../materials.
    Position everything relative to group origin (base position handled by parent).

    Create `geometry/index.ts` with barrel export of TeamABase.
  </action>
  <verify>
    - `pnpm build --filter @voxel-strike/client` compiles
    - TeamABase exports correctly from index.ts
  </verify>
  <done>
    TeamABase component created with platforms, flag zone, and tech aesthetic
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Team A base into map</name>
  <files>
    apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx
  </files>
  <action>
    Update SciFiCTFMap.tsx to render TeamABase:

    1. Import TeamABase from ./geometry
    2. Render <TeamABase /> inside the main group
    3. TeamABase handles its own positioning based on config

    The map should now show:
    - Grid floor (from Plan 01)
    - Team A base geometry on the left side
  </action>
  <verify>
    - Run game and visually confirm Team A base appears on left side
    - Platforms visible at different heights
    - Red/orange glow accents visible
    - Flag zone platform clearly marked
  </verify>
  <done>
    Team A base renders in game with distinct tech/platform aesthetic
  </done>
</task>

</tasks>

<verification>
- `pnpm build --filter @voxel-strike/client` compiles successfully
- Team A base visible on left side of map when game runs
- Multiple elevation levels present (ground, y=1 flag zone, y=3 command)
- Red/orange glow accents visible on edges and pillars
- Three route openings facing center
</verification>

<success_criteria>
1. TeamABase component exists and exports from geometry/index.ts
2. SciFiCTFMap renders TeamABase
3. Base has main platform, elevated platform, and flag zone
4. Tech aesthetic visible (glowing accents, pillars)
5. Three route connection points defined
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-foundation/01-02-SUMMARY.md`
</output>
