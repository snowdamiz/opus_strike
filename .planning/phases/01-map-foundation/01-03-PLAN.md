---
phase: 01-map-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/client/src/components/game/maps/sci-fi-ctf/geometry/TeamBBase.tsx
  - apps/client/src/components/game/maps/sci-fi-ctf/geometry/index.ts
  - apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx
autonomous: true

must_haves:
  truths:
    - "Team B side has distinct natural/cave visual identity"
    - "Team B base area contrasts clearly with Team A"
    - "Team B side has clear spawn area and flag zone position"
  artifacts:
    - path: "apps/client/src/components/game/maps/sci-fi-ctf/geometry/TeamBBase.tsx"
      provides: "Team B base geometry"
      exports: ["TeamBBase"]
  key_links:
    - from: "apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx"
      to: "apps/client/src/components/game/maps/sci-fi-ctf/geometry/TeamBBase.tsx"
      via: "import and render at TEAM_B_BASE position"
      pattern: "<TeamBBase"
---

<objective>
Create Team B base geometry with natural/cave sci-fi aesthetic.

Purpose: Establish the right side of the map with a distinct natural aesthetic that contrasts with Team A's tech platforms, featuring cave-like structures, organic shapes, and cool blue/cyan accents.

Output: TeamBBase component rendering cave structures, rocky platforms, and natural elements with blue/cyan glowing accents.
</objective>

<execution_context>
@~/.claude/agents/gsd-planner.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-map-foundation/01-CONTEXT.md
@.planning/phases/01-map-foundation/01-RESEARCH.md
@apps/client/src/components/game/maps/sci-fi-ctf/materials.ts
@apps/client/src/components/game/maps/sci-fi-ctf/config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Team B base geometry</name>
  <files>
    apps/client/src/components/game/maps/sci-fi-ctf/geometry/TeamBBase.tsx
  </files>
  <action>
    Create `geometry/TeamBBase.tsx` with natural/cave aesthetic:

    1. Main base floor (spawn area):
       - Irregular-looking floor at y=0, overall ~30x40 footprint
       - Use caveMaterial for the surface (rocky texture)
       - Add teamBAccent glow strips embedded in the rock (bioluminescent feel)
       - Create slight height variation with multiple overlapping floor pieces

    2. Cave overhang structure:
       - Large overhanging rock formation providing cover
       - Position at back of base, extending over spawn area
       - Use caveMaterial, add teamBGlow crystals/formations underneath
       - Creates sense of being inside a cavern

    3. Flag zone alcove:
       - Recessed area at y=0.5 (slightly raised rocky platform)
       - Surrounded by rock walls on three sides (protective alcove)
       - Use teamBGlow for crystal formations marking the zone
       - This is where the flag will be placed

    4. Natural formations:
       - Add 2-3 large rock columns/stalagmites
       - Varying heights (2-5 units)
       - Positioned to create interesting sightlines but not block main paths
       - Add small glowing crystal clusters at bases

    5. Route connection points:
       - Three cave mouths facing center: top (north), middle, bottom (south)
       - Each opening ~8-10 units wide
       - Cave mouths have irregular edges (not straight boxes)
       - Mark openings with teamBGlow crystal strips on floor

    Component structure:
    ```tsx
    export function TeamBBase() {
      return (
        <group position={[TEAM_B_BASE.x, 0, TEAM_B_BASE.z]}>
          {/* Main spawn floor (rocky) */}
          {/* Cave overhang */}
          {/* Flag zone alcove */}
          {/* Rock formations */}
          {/* Route markers */}
        </group>
      );
    }
    ```

    Use boxGeometry for all shapes (even "organic" shapes are low-poly boxes at angles).
    Import materials from ../materials.
    Position everything relative to group origin.
  </action>
  <verify>
    - `pnpm build --filter @voxel-strike/client` compiles
    - TeamBBase exports correctly
  </verify>
  <done>
    TeamBBase component created with cave structures, flag alcove, and natural aesthetic
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Team B base into map</name>
  <files>
    apps/client/src/components/game/maps/sci-fi-ctf/geometry/index.ts
    apps/client/src/components/game/maps/sci-fi-ctf/SciFiCTFMap.tsx
  </files>
  <action>
    1. Update geometry/index.ts:
       - Add TeamBBase to barrel exports

    2. Update SciFiCTFMap.tsx to render TeamBBase:
       - Import TeamBBase from ./geometry
       - Render <TeamBBase /> inside the main group
       - TeamBBase handles its own positioning based on config

    The map should now show (assuming Plan 02 also complete):
    - Grid floor
    - Team A base (left side, tech)
    - Team B base (right side, natural)

    If Plan 02 is not complete yet (parallel execution), just add TeamBBase.
  </action>
  <verify>
    - Run game and visually confirm Team B base appears on right side
    - Natural/cave aesthetic clearly different from Team A
    - Blue/cyan glow accents visible
    - Flag zone alcove clearly marked
    - Three route openings visible
  </verify>
  <done>
    Team B base renders in game with distinct natural/cave aesthetic
  </done>
</task>

</tasks>

<verification>
- `pnpm build --filter @voxel-strike/client` compiles successfully
- Team B base visible on right side of map when game runs
- Cave overhang structure present
- Blue/cyan glow accents visible (crystals, floor strips)
- Visual contrast clear between Team A and Team B sides
- Three route openings facing center
</verification>

<success_criteria>
1. TeamBBase component exists and exports from geometry/index.ts
2. SciFiCTFMap renders TeamBBase
3. Base has cave floor, overhang, and flag alcove
4. Natural aesthetic visible (rocky materials, crystal glows)
5. Clear visual contrast with Team A's tech aesthetic
6. Three route connection points defined
</success_criteria>

<output>
After completion, create `.planning/phases/01-map-foundation/01-03-SUMMARY.md`
</output>
